<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/search_menu"
        android:icon="@drawable/ic_search"
        android:title="@string/search.title"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="collapseActionView|ifRoom"/>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/sort_manual_menu"
            android:icon="@drawable/ic_account"
            android:title="@string/menu.order.manual"
            app:showAsAction="never"/>
        <item
            android:id="@+id/sort_rating_menu"
            android:icon="@drawable/ic_account"
            android:title="@string/menu.order.rating"
            app:showAsAction="never"/>
        <item
            android:id="@+id/sort_code_lines_menu"
            android:icon="@drawable/ic_people"
            android:title="@string/menu.order.code_lines"
            app:showAsAction="never"/>
    </group>
</menu>